Environment:

	Build Tool: gradle

	IDE: Eclipse Luna

Total Design:

	Entry point:
		Program.java

	Package servie:
		the facade service.

	Package service.entity:
        	the entities used by facade service.

	Package service.map:
		used to contain classes implement map and visitor.

	Package service.map.entity:
		the entities for a map.

	Package service.map.visitor:
        	the classes used to implement visiting in a map.